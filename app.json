{
  "name": "greenfield-project",
  "scripts": {},
  "env": {},
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "npm install supertest mocha chai jest-cli babel-jest babel-preset-react react-test-renderer jest-css-modules",
        "test": "jest --forceExit & mocha test --exit"
      }
    }
  },
  "formation": {},
  "addons": [],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}
